# Neon PostgreSQL (use pooled URL)
DATABASE_URL=postgresql://user:password@host/neondb?sslmode=require

# Admin login (server-side only — never expose in VITE_*)
ADMIN_PASSWORD=detalhe2024

# JWT secret for admin sessions (use a long random string in production)
JWT_SECRET=change-this-to-a-random-secret

# Optional: API URL for frontend (leave empty in dev — Vite proxies /api)
# VITE_API_URL=https://your-api.example.com
